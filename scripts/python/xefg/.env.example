# Copy this to scripts/python/xefg/.env (or rely on repo-root .env)
# DATABASE_URL must point to the same Postgres your Next.js app uses.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Optional: override the season the extractor pulls. Defaults to 2025.
# XEFG_SEASON=2025
